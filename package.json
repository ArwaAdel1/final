{
  "name": "shop-project",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cd backend && npm run dev",
     "start": "cd backend && npm install && node server.js",
    "seed": "cd backend && npm run seed"
   
  }
}
